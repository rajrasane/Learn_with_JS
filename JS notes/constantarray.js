// Constant Array 
// - in we want to make a constant array in js , so that later it cannot be change , then we const keyword while declaring it
// - but if we push or pop it , then the changes will occur in the existing array , but we cannot insert a new array elements/values
// in the same variable 

const arr = [1,2,3,4,5]
console.log(arr);

arr = [1,2,3,4,5,6];